#include <stdio.h>

	int	main() {

	int num;

	printf("Digite um n.: ");
	scanf("%d", &num);


	if (num >= 10 && num <= 50) {
	printf(" O n. é: %d\n", num);
	}

	else if (num >= 20 && num <= 60) {
	printf(" O n. é: %d\n", num);

	}
